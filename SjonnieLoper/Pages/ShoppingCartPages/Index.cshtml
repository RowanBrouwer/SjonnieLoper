@page
@model SjonnieLoper.Pages.ShoppingCartPages.IndexModel
@{
}

<h4>Your ShoppingCart</h4>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Selected amount</th>
            <th>Whiskey</th>
            <th class="text-right">Price</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var line in Model.ShCartItems)
        {
        <tr>
            <td>@line.Amount</td>
            <td>@Html.DisplayFor(c => line.Whiskey.Name)</td>
            <td class="text-right">€@Html.DisplayFor(c => line.Whiskey.Price)</td>
            <td class="text-right">€@((line.Amount * line.Whiskey.Price).ToString())</td>
        </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                €@Model.PriceTotal.ToString()
            </td>
        </tr>
    </tfoot>
</table>